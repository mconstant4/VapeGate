<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <SwitchPreference
        android:id="@+id/enable_vapegate"
        android:key="vapegate_enable"
        android:defaultValue="false"
        android:title="Enable VapeGate"/>
    <SwitchPreference
        android:id="@+id/daily_drags"
        android:key="daily_drags"
        android:defaultValue="0"
        android:title="Enable VapeGate"/>
</PreferenceScreen>